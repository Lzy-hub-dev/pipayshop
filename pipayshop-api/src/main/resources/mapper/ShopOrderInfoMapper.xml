<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pipayshopapi.mapper.ShopOrderInfoMapper">


    <select id="selectShopOrders" resultType="com.example.pipayshopapi.entity.vo.ShopOrderInfoVO">
        select o.order_id,o.transaction_amount,o.commodity_id,
                o.uid,o.shop_id,o.create_time,c.commodity_img_list,c.commodity_name
        from shop_order_info o
        left join shop_commodity_info c
        on o.commodity_id=c.commodity_id
        where o.uid=#{userId} and o.del_flag=0
    </select>

</mapper>
